[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
markers =
    integration: live HTTP + Postgres (+ Redis for RQ); set RUN_INTEGRATION=1
filterwarnings =
    ignore::DeprecationWarning
